## Optional — method chaining
Quand un repository retourne Optional, on peut enchaîner :
- .filter() pour vérifier une condition
- .map() pour transformer la valeur
- .orElseThrow() pour lever une exception si vide

Exemple : login service